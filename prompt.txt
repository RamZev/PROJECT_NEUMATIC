===============================================
Sanear la tabla producto
===============================================
0.- Hay registros cuyo campo "nombre_producto" contiene "PRODUCTO SIN DESCRIPCION...".
	Decidir qué hacer con esos registros, eliminarlos?.
	
1.- Depurar los registros según "tipo_producto".
	Hay registros con tipo_producto="P" y son servicios.
	
2.- Hay registros cuyo campo "cai" contienen información pero no cuentan con un "id_cai_id".
	Registrar el cai en la tabla producto_cai y asignar el id de este al correspondiente registro en la tabla producto.
	
3.- Crear una rutina que por cada producto con tipo_producto="P" se cree un registro por cada depósito en producto_stock y si 
	además tiene cai, se haga lo mismo en la tabla producto_minimo.
	
4.- Asignar el correspondiente id_alicuota_iva.
	El campo alicuota_iva tiene la alícuota pero el campo id_alicuota_iva tiene el id 1 en todos los registros.
	
*.- Eliminar el campo "cai".
	Este cai es el que ya estaba en la tabla producto y se dejó para poder asignarle el id correspondiente de la tabla producto_cai.



==================================================================================================================================

Qué me puedes sugerir para lo siguiente:

En mi proyecto Django tengo una opción que me permite consultar los datos de mi modelo Producto y exportarlos a un archivo Excel, 
luego el usuario modifica los datos que requiere modificar en ese archivo Excel y por  último, debe cargar ese archivo Excel para 
actualizar los registros modificados en la base de datos.

Ya tengo hecha la primera parte que consulta y genera el archivo Excel, ahora necesito hacer la funcionalidad que me permita ubicar
y cargar el archivo Excel ya modificado, que me muestren esos registros del archivo Excel en pantalla en una tabla y me muestre en un 
panel derecho los campos (títulos de las columnas) precedidos de un check para marcar, indicando así, los campos que se deben actualizar
en la base de datos.

Esta funcionalidad me gustaría que se implemente empleando Vistas Basadas en Clases de ser posible, porque así he venido trabajando en
mi proyecto. Deseable también, crear un formulario (que herede del formulario base que te comparto: CrudGenericForm en el 
módulo: crud_forms_generics.p) para poder validar la entrada de datos (archivo Excel y campos a actualizar). 

Vamos por partes, primero dame la implementación solo hasta mostrar el fromulario para ubicar y cargar el archivo Excel y mostrar los 
registros en la tabla, luego que eso funcione te digo para implementar la funcionalidad de actualizar los datos en la base de datos.
Qué me puedes sugerir?